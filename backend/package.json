{
  "name": "@webstudy/backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/app.ts --platform=node --format=cjs --outdir=dist",
    "prestart": "yarn build",
    "start": "node dist/app.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^3.6.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "esbuild": "^0.14.2",
    "prisma": "^3.6.0"
  }
}
